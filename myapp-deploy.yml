apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-feature-c
  labels:
    app: myapp-feature-c
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp-feature-c
  template:
    metadata:
      labels:
        app: myapp-feature-c
    spec:
      containers:
      - name: myapp-feature-c
        image: 366449471548.dkr.ecr.ap-south-1.amazonaws.com/myapp-feature-c:latest
        ports:
        - containerPort: 3000
      imagePullSecrets:
      - name: aws-registry
